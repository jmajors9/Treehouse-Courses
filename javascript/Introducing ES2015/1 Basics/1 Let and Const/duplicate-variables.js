'use strict';

// uh oh!
// var student = { name: 'Ken' };
// var student = { name: 'James' };

// run this and you only get 'James'!
// so now we can use let or const

const student = { name: 'Ken' };
var student = { name: 'James' };

// so this will throw an error now.

console.log(student);
