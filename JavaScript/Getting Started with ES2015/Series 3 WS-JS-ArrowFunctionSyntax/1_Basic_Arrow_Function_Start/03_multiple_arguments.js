/* jshint esversion: 6 */
// function multiply(x, y) {
//     return x * y;
// }

// function add(a, b) {
//     return a + b;
// }

// function subtract(a, b) {
//     return a - b;
// }

/*
arrow styles

const multiply = (x, y) => {
    return x * y;
}

const add = (a, b) => {
    return a + b;
}

const subtract = (a, b) => {
    return a - b;
}
*/

/* cleaning up. have to leave () because of mult vars*/
const multiply = (x, y) => x * y;
const add = (a, b) => a + b;
const subtract = (a, b) => a - b;

