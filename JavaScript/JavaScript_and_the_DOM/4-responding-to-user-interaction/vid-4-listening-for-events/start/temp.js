/* jshint esversion: 6 */

window.setTimeout((something) => {
  console.log(something);
}, 3000, 'Greetings, everyone!');

// listening to user actions
// target.addEventListener(type, listener[, options]);
// type is a string for the even type
// listener  - the obj that receives a notification when the event occurs

