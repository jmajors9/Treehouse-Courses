using System;
using System.Collections.Generic;

namespace Treehouse.CodeChallenges
{
  public static class MathHelpers
  {
    public static List<int> GetPowersOf2(int powerBase)
    {
      // take powerBase, create list of that length
      // foreach through the powerBase list and raise each to the power of 2
      // return

      List<int> baseList = new List<int>(powerBase);

      return;

    }
  }
}

/*
I didn't solve it.
*/

